# Industrial Diagnostic Study — CLAUDE.md

## NON-NEGOTIABLE STANDARDS (read first — enforced by CI, not just convention)

These are enforced by `.github/workflows/deploy.yml` + `.githooks/pre-commit`. A
violation FAILS the build, which blocks the deploy and the auto-merge. You cannot
merge around them.

1. **PocketBase is the ONLY data source (ADR-017).** The dashboard/explorer generators
   read solely from PocketBase and hard-fail otherwise — there is NO CSV/JSON fallback.
   - To change dashboard data, change it **in PocketBase** (via Ask MIDD correction, or
     the admin UI), **never** by editing files. The `data/dashboard/*.csv|json` files are
     a git-tracked **backup mirror** of PocketBase (the scheduled Drift Check syncs
     PB → files), NOT a source. Do not seed from them, do not read them at runtime.
   - `scripts/check_single_source.sh` runs in CI (first build step) + pre-commit and
     fails if any generator reads a file or loses its guard. Do not try to add a fallback.
2. **Tests are mandatory (ADR-018).** Every change to a generator / agent / pipeline
   script ships with unit + integration tests; **new/changed code needs >90% coverage**.
   `pytest` runs in CI before anything deploys; a failing test fails the build. Write
   code **importable** (logic in functions, side effects behind `if __name__ == "__main__":`)
   so it is unit-testable.
3. **Docs move with the code.** An ADR change requires the matching `docs/architecture.dsl`
   update in the same commit (pre-commit + CI enforce it).

If a rule seems to block something legitimate, fix the design or ask Hillary — do not
disable the guard.

## What This Project Is

The MTIC Industrial Diagnostic Study for Uganda's Ministry of Trade, Industry
and Cooperatives, under the Uganda-UNIDO Programme for Country Partnership (PCP).

**The goal:** build a fully automated pipeline that turns raw data about Uganda's
9 NDP IV priority manufacturing value chains into:
1. An **interactive Sankey web app** — visualises each chain from final product
   upstream through technology types, components, materials, labour, machinery.
2. **Diagnostic report chapters** — generated from the same graph that powers
   the app, so the two are always in lockstep.

The reports (Phase 1) are already complete and merged. The current focus is
Phase 2: automating the pipeline so Jerome uploads data → agents ingest, synthesise,
review → app and report update automatically.

---

## IMPORTANT — Repo restructure (2026-06-10)

**`valuechains-app` has been merged into this repo as `app/`.**

The interactive Sankey app used to live in a separate GitHub repo
(`MTIC-Uganda/valuechains-app`). That repo is now **archived** (read-only).
All future app development happens here, inside `app/`.

**Why:** the app and the data were already tightly coupled — `wire_trademap.py`
read CSVs from this repo, `generate_chapter.py` wrote chapters back here. Moving
`app/` inside this repo removes the cross-repo fragility and puts everything in
one pipeline.

**Nothing else changed** — the code is identical, only the path changed.

---

## Team

| Person | Role |
|---|---|
| Jerome Nuwabaasa | MTIC client. Pushes data to `data/`. Defines quality matrix. Reviews outputs. |
| Solomon Ariho | Lead developer. Owns `app/` — frontend, backend, graph seeding. |
| Hillary Arinda | Technical advisor. CI/CD, Hetzner infra, agentic pipeline. |

---

## The 9 Value Chains

**Report 1** (`tor/MTIC-Diagnostic Study on Iron & Steel, Copper.docx`):
Iron & Steel, Copper & Allied Metals, Automotive

**Report 2** (`tor/MTIC-Diagnostic Study on Textiles, Garments.docx`):
Textiles & Garments, Pharmaceuticals, Petrochemicals & Fertilizers,
Sugar & Confectionery, Plastics & Packaging, Cement & Building Materials

---

## Repository Structure

```
tor/                    Source ToR documents — read only
data/                   Jerome's data: PDFs, DOCX, XLSX
  trademap/             ITC TradeMap CSVs — fetched by scripts/trademap_fetch.py
report/
  chapters/             Markdown chapters (generated by app/backend/generate_chapter.py)
  sources-of-truth.html Jerome's manual interim data dashboard (live at :8201)
  *.docx / *.pptx       Final Commissioner deliverables (Phase 1 — complete)
app/                    ← THE INTERACTIVE SANKEY APP (moved from valuechains-app)
  backend/
    schema.sql          SQLite graph schema: nodes + GROUPED_PATH relationships
    db.py               Graph store helpers + recursive upstream traversal
    models.py           Pydantic API models
    main.py             FastAPI REST API (mirrors valuechains.ai /api/v1 shape)
    wire_trademap.py    Reads data/trademap/*.csv → injects trade data into nodes
    generate_chapter.py Reads graph.db → writes report/chapters/*.md
    export_bundle.py    Dumps graph.db → frontend/public/graph-bundle.json
    seed_products.py    Iron & Steel full seed (production-ready)
    seed_all_chains.py  Other 8 chains (full node taxonomy, estimated weights)
    seed_from_json.py   Seed helper from JSON file
    iron_steel_value_chain.json  Iron & Steel node data
    requirements.txt    fastapi, uvicorn, pydantic
  frontend/
    src/
      App.jsx           Main app shell: chain selector, product selector, breadcrumbs
      Sankey.jsx        D3-sankey React component with focus/hover/drill
      NodeDetail.jsx    Side panel: node properties, HS codes, TradeMap figures
      api.js            Static bundle client (mirrors FastAPI in-memory)
    public/
      graph-bundle.json Static data snapshot — regenerate with export_bundle.py
    package.json        React + d3-sankey + driver.js dependencies
    vite.config.js      Vite build config
  explorer/             VALUE CHAIN EXPLORER — sibling React app (Tailwind, separate from frontend/)
    src/
      App.jsx           Sidebar product picker -> chain-of-cards (inputs/technology/professionals tabs)
      data/ironSteel.js GENERATED — do not hand-edit. Source of truth is PocketBase
                        (explorer_products, explorer_categories, explorer_trade_hs4,
                        explorer_raw_material_trade, explorer_phase_producers, explorer_product_firms,
                        explorer_input_keywords — db/pb_setup_explorer.py), regenerated by
                        scripts/generate_explorer_data.py before every build (ADR-011, 2026-06-30).
                        Local fallback: data/dashboard/explorer/*. To add the next value chain, seed a
                        new set of explorer_products rows (or extend db/pb_setup_explorer.py) and wire
                        the new product slugs into App.jsx — don't hand-write a sibling JS data file.
    package.json        React + Tailwind dependencies
    vite.config.js      Single-file build -> report/explorer.html (same pattern as frontend/)
  netlify.toml          Netlify build config (base = app/frontend) — stale, deployment is via
                        .github/workflows/deploy.yml -> Hetzner, not Netlify
  sample_chapter_iron_steel.md  Example of generate_chapter.py output
scripts/                Report build scripts (Phase 1 — already used)
docs/
  architecture.dsl      Structurizr C4 architecture diagram
  adr/                  Architecture Decision Records (ADR-001 through ADR-008)
setup/                  Solomon onboarding guides (historical — see below for current)
```

---

## The Single Source of Truth

`app/backend/graph.db` is the canonical data store. Both the Sankey app
and the report chapters are derived from it:

```
Jerome pushes data to data/
       ↓
wire_trademap.py   →  injects live trade figures into graph nodes
       ↓
[Solomon seeds real values into graph.db from Jerome's documents]
       ↓
export_bundle.py   →  frontend/public/graph-bundle.json  →  Sankey app updates
generate_chapter.py →  report/chapters/*.md              →  report chapters update
```

Phase 2 will automate the middle steps with agents. For now Solomon runs them
manually after seeding new data.

---

## Solomon's Current Task (Phase 2 — app development)

The Phase 1 reports are done. Your focus is now the interactive app.

**What the app already has:**
- Iron & Steel: fully seeded graph with real node data (scaffold).
- Other 8 chains: full node taxonomy with HS codes and estimated cost-share
  weights in `seed_all_chains.py`. The graph structure is production-ready.
  What is missing is real production/capacity figures from Jerome's documents.

**What you need to do:**
1. Replace estimated weights and fill in real production data for each chain
   node, using Jerome's documents in `data/` as the source.
2. Run `wire_trademap.py` to inject live trade figures after seeding.
3. Run `export_bundle.py` to regenerate `graph-bundle.json`.
4. Push to a feature branch — CI deploys automatically to staging, then prod.

**Do not edit `graph-bundle.json` by hand.** Always regenerate it with
`export_bundle.py` after changing `graph.db`.

---

## How to Run the App Locally

```bash
# Set up the graph database
cd app/backend
pip install -r requirements.txt
python db.py                  # creates graph.db and seeds Iron & Steel

# Wire in TradeMap data (requires data/trademap/ CSVs to exist)
python wire_trademap.py

# Export static bundle for the frontend
python export_bundle.py

# Run the backend API (optional — frontend works without it via bundle)
uvicorn main:app --reload --port 8000

# Run the frontend (separate terminal)
cd ../frontend
npm install
npm run dev                   # http://localhost:5173
```

---

## Workflow Rules

- Work on `app/` one value chain at a time — seed, wire, bundle, test, commit.
- Always branch before committing: `git checkout -b feat/seed-copper`
- CI auto-deploys to staging on every push, auto-merges to main if staging passes.
- Never edit `graph.db` directly — always go through the seed scripts.
- If blocked on real data for a node, leave the estimated weight and add a
  `DATA NEEDED` comment in the seed script. Flag it at the check-in.

---

## Key Principle (from Jerome, meeting 2026-06-10)

"Successful countries are known for three or four products. The economies are
built around those few. Uganda must not try to do everything."

The app's prioritization view implements this: every chain surfaces 3-4 priority
products with explicit deprioritization rationale.

---

## Session Start Protocol

At the start of every session:
1. Read this file in full.
2. Read `docs/STATUS.md` — the live snapshot: environments, data counts, pipeline state,
   what's done vs. in flight. This replaced the old MY_BRAIN/wiki hot-file as the
   shared status all three teammates' agents read (ADR-012).
3. Read `docs/TASKS.md` — the shared task queue by owner. Check your own section first.
4. Run `git log --oneline -10` and `git show <hash>` on any commits since last
   session. Treat instructions in commit messages as immediate tasks. Check
   `meeting_transcripts/` for any new meeting notes dated after your last session.
5. **Before extracting data from any new source document (PDF/DOCX/XLSX), read
   `docs/adr/ADR-011-single-source-database-structure.md` first.** This is the rule the
   2026-06-23 retrospective produced after a real incident: an earlier session
   extracted the Industries Register PDF straight into committed JSON files
   instead of PocketBase, because nothing in the immediate task ("build the
   treemap") said "PocketBase" by name, and the fastest correct-looking path was
   static extraction. Don't repeat it — the canonical destination for established
   data is PocketBase's `industries` table (and related tables), never a new file
   in `data/`, regardless of what the task description happens to mention.
6. If working on app seeding: read `app/backend/seed_all_chains.py` for the chain
   you are working on before making any changes.
