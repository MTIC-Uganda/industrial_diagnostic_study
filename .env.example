# Copy to .env and fill in your values. Never commit .env to git.

# ── PocketBase (data backend) ─────────────────────────────────────────────────
# PB_URL is the only secret CI needs (public read, no auth required)
PB_URL=http://89.167.121.193:8090

# Admin credentials — only needed to run db/pb_setup.py (one-time seed)
# Never put these in CI or share them.
PB_ADMIN_EMAIL=
PB_ADMIN_PASSWORD=
