# Test + coverage tooling (ADR-018: unit + integration tests, >90% on new/changed code)
pytest>=8.0
pytest-cov>=5.0
diff-cover>=9.0
# Runtime libs the tests import: the analytics sandbox (ADR-020) exercises pandas/
# numpy/scikit-learn/matplotlib, and the Ask MIDD app tests use fastapi + httpx.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
fastapi>=0.110
httpx>=0.27
