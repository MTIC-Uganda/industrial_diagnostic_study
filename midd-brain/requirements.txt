# Ask MIDD brain runtime deps. The analytics sandbox (ADR-020) needs the scientific
# stack; if these are absent the analytics path degrades silently to the normal
# repo-scoped answer, so the brain still runs without them.
fastapi>=0.110
uvicorn>=0.27
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
statsmodels>=0.14
matplotlib>=3.7
